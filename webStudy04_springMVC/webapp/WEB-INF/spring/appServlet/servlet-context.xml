<?xml version="1.0" encoding="UTF-8"?>
<beans:beans 
	xmlns="http://www.springframework.org/schema/mvc"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:c="http://www.springframework.org/schema/c"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd">
	<!-- 한방에 모든것들을 등록하기 -->
	<annotation-driven validator="validator"/>
	
	<beans:bean id="messageSource" class="org.springframework.context.support.ResourceBundleMessageSource" 
		p:basenames="kr.or.ddit.messages.message,kr.or.ddit.messages.errorMessages"
	/>
	
	<!-- 	validate -->
	<beans:bean id="validator" class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean" 
		p:validationMessageSource-ref="messageSource"
	/>
	
	
<!-- 	정적 자원에 대한 처리 -->
	<!-- resources 안트패스매핑방식 -->
	<resources location="/resources/" mapping="/resources/**" cache-period="0"/>
	<!-- 정적자원에 대한 제어권을 다시 돌려주는것. -->
	<default-servlet-handler/>
	
	<!-- 기본으로 등록된 것이 마음에 들지 않으면 다시 등록하면된다. -->
<!-- 	viewResolver에 prefix와 suffix가 없으니까 -->
<!-- 	한가지 전략에 두가지 이상이 존재한다면 반드시 우선순위를 정해주어야 한다. -->
<!-- 	IRVR은 항상 순서가 가장 마지막. 못찾으면 404를 내보낸다. 다음 resolver에게 가지않는다 -->
<!-- 	oder를 따로 주지 않으면 거의 항상 제일 마지막(인티저로 줄 수 있는 제일 큰 값) -->
	<beans:bean id="IRVR" class="org.springframework.web.servlet.view.InternalResourceViewResolver"
		p:prefix="/WEB-INF/views/"
		p:suffix=".jsp"
	/>
<!-- 	등록되어있는 빈에서 view를 찾는다. -->
	<beans:bean id="BNVR" class="org.springframework.web.servlet.view.BeanNameViewResolver"
		p:order="1"
	/>
	<!-- tiles로 나오는 녀석 resolver로 새로 등록하기 -->
	<beans:bean id="TVR" class="org.springframework.web.servlet.view.tiles3.TilesViewResolver"
		p:order="2"
	/>
	
<!-- tiles에 쓰는 동적인 녀석 빈으로 등록하기 -->
<!--  어노테이션 하나 만ㄷ들어서 작성함. -->
<!-- 	<beans:bean id="boardViewPreparer" class="kr.or.ddit.board.preparer.BoardViewPreparer"></beans:bean> -->
	
	<!-- tiles xml파일 등록 -->	
	<beans:bean class="org.springframework.web.servlet.view.tiles3.TilesConfigurer"
		p:definitions="/WEB-INF/tiles/*-definitions.xml"
		p:preparerFactoryClass="org.springframework.web.servlet.view.tiles3.SpringBeanPreparerFactory"
	/>
	
	<beans:bean id="downloadView" class="kr.or.ddit.board.view.DownloadView" />
	
<!-- 	파일업로드 시 파일이 정상적으로 올라가지 않다면 이 녀석을 등록해야한다. -->
<!-- 	몇몇 bean은 id를 반드시 정해진 형식으로 해야한다. handlerAdapter가 injection을 하기 때문 -->
<!-- 	원본요청을 필터링해서 multipartServletRequest로 wrapping -->
	<beans:bean id="multipartResolver" class="org.springframework.web.multipart.support.StandardServletMultipartResolver" />
	
	<context:component-scan base-package="kr.or.ddit" use-default-filters="false" >
		<context:include-filter type="annotation" 
			expression="org.springframework.stereotype.Controller"/>
		<context:include-filter type="annotation" 
			expression="org.springframework.web.bind.annotation.ControllerAdvice"/>
		<context:include-filter type="annotation" 
			expression="kr.or.ddit.annotation.ViewPreparer"/>
	</context:component-scan>
	
<!-- 	핸들러 인터셉터 설정 -->
	<beans:bean id="webContentInterceptor" class="org.springframework.web.servlet.mvc.WebContentInterceptor"
		p:cacheSeconds="0"
		p:useCacheControlHeader="true"
		p:useCacheControlNoStore="true"
		p:useExpiresHeader="true"
	/>
<!-- 	쿠키에 저장해주기 -->
	<beans:bean id="localeResolver" class="org.springframework.web.servlet.i18n.CookieLocaleResolver" 
		p:cookieName="langCookie"
		p:cookiePath="/"
		p:cookieMaxAge="#{60*60*24*3}"
	/>
	<interceptors>
		<beans:ref bean="webContentInterceptor"/>
		<beans:bean class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor"
			p:paramName="lang"
		/>
	</interceptors>
	
</beans:beans>
